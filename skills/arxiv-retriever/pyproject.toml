[project]
name = "arxiv-retriever"
version = "0.1.0"
description = "Extract search parameters from user prompt and download arXiv papers"
requires-python = ">=3.7"
dependencies = [
    "arxiv",
    "arxiv2bib",
]

[tool.uv]
dev-dependencies = []
